# Python dependencies for mini_trt_llm/tools/convert/hf_to_mini_trt_llm.py
# 用于把 HuggingFace / PyTorch checkpoint 转换为 mini_trt_llm 的 config.json + model.safetensors。

transformers>=4.40
safetensors>=0.4
torch>=2.0

# 图结构探针（mini_trt_llm/tools/inspect_onnx.py）用它读 ONNX 的算子分布与子图形态。
onnx>=1.16
